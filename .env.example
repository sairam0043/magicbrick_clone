# MongoDB connection (optional – defaults to localhost if not set)
MONGODB_URI=mongodb://localhost:27017/realestateguru

# Only if you run the app on a different host (e.g. production)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
